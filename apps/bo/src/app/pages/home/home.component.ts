import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  standalone: false, // keep standalone if needed
  templateUrl: './home.component.html',
  styleUrl: './home.component.scss',
})
export class HomeComponent {}
